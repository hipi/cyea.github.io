language: node_js
node_js: stable
cache:
  directories:
    - node_modules
install:
  - yarn
script:
  - yarn g
before_install:
  - export TZ='Asia/Shanghai'
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- -- version 0.23.2
  - export PATH="$HOME/.yarn/bin:$PATH"
after_success:
  - cd ./public
  - git init
  - git config user.name "cyea"
  - git config user.email "yuyehack@gmail.com"
  - git add .
  - t=`date '+%Y-%m-%d %H:%M:%S'`
  - git commit -m "Update site by travis-ci from branch icuras $t"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
branches:
  only:
    - icarus
env:
  global:
    - GH_REF: github.com/cyea/yoaio.com.git
    - secure:
